<Window x:Class="Minesweeper_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper_WPF"
        mc:Ignorable="d"
        Title="Aknakereső" Height="505" Width="460" Icon="/icon.ico">

    <Grid>
        <!-- Háttér -->
        <Grid.Background>
            <ImageBrush ImageSource="/hatter.png" Stretch="UniformToFill"/>
        </Grid.Background>
        <!-- Sorok: menü, pálya, alsó panel -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menü -->
            <RowDefinition Height="*"/>
            <!-- Pálya -->
            <RowDefinition Height="Auto"/>
            <!-- Alsó panel -->
        </Grid.RowDefinitions>
        <!-- Menü -->
        <Menu Grid.Row="0" Background="White">
            <MenuItem Header="Játék">
                <MenuItem Header="Új játék" x:Name="NewGame" Click="NewGame_Click"/>
                <Separator/>
                <MenuItem Header="Statisztika" x:Name="Stats" Click="Stats_Click"/>
                <MenuItem Header="Beállítások" x:Name="Settings" Click="Settings_Click"/>
                <MenuItem Header="Megjelenés módosítása" x:Name="AppearanceMenu" Click="Appearance_Click"/>
                <Separator/>
                <MenuItem Header="Kilépés" x:Name="Exit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="Súgó">
                <MenuItem Header="Súgó megjelenítése" x:Name="Help" Click="Help_Click"/>
                <Separator/>
                <MenuItem Header="Az aknakereső névjegye" x:Name="About" Click="About_Click"/>
                <Separator/>
                <MenuItem Header="További játékok az interneten" x:Name="MoreGames" Click="MoreGames_Click"/>
            </MenuItem>
        </Menu>
        <!-- Pálya külön ViewBoxban (ne adj fix Width/Height-et a UniformGridnek) -->
        <Viewbox x:Name="BoardView" Grid.Row="1" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
            <UniformGrid x:Name="GameBoard" Rows="5" Columns="10" />
        </Viewbox>
        <!-- Alsó információs sáv: szélessége kötve a pálya (BoardView) aktuális szélességéhez -->
        <DockPanel Grid.Row="2" HorizontalAlignment="Center" Width="{Binding ActualWidth, ElementName=BoardView}" Margin="0,10,0,10" LastChildFill="True">
            <!-- Bal csoport: a bal szélhez rögzítve -->
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="/Assets/Images/GUI/Icons/clock.png" Width="40" Height="40" Margin="0,0,8,0"/>
                <Border Background="#31569C" BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="8,4" MinWidth="80" VerticalAlignment="Center">
                    <TextBlock x:Name="TimerDisplay" Text="0" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
            <!-- Jobb csoport: a jobb szélhez rögzítve -->
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#31569C" BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="8,4" MinWidth="80" VerticalAlignment="Center" Margin="0,0,8,0">
                    <TextBlock x:Name="MineCounter" Text="10" Foreground="White" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Image Source="/Assets/Images/GUI/Icons/flower.png" Width="40" Height="40"/>
            </StackPanel>
            <!-- Középső kitöltő: üres Grid, hogy a bal és jobb csoport a széleken maradjon -->
            <Grid/>
        </DockPanel>
    </Grid>
</Window>
